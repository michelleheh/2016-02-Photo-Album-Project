var AppModel = Backbone.Model.extend({

  initialize: function(params){
    // this.set('currentImage', new ImageModel());
    // this.set('imageList', new ImageList());
    // need to add events
    // params.imageList.on('click', function(image) {
    //   this.set();
    // }, this);
  }

});